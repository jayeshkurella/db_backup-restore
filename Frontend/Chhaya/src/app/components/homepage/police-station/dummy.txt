<div class="container-fluid mt-2">
  <nav class="navbar navbar-expand-lg navbar-light bg-dark">
    <div class="container-fluid d-flex justify-content-center">
      <a class="navbar-brand text-light" href="#">Search Police Station </a>
      <form class="d-flex flex-wrap justify-content-center gap-2 mx-auto mt-2 " (ngSubmit)="onSearch()">
        <input 
          class="form-control me-2" 
          type="text" 
          placeholder="Search by Name" 
          aria-label="Search Name" 
          [(ngModel)]="searchName" 
          name="searchName"
        >
        <input 
          class="form-control me-2" 
          type="text" 
          placeholder="Search by City" 
          aria-label="Search City" 
          [(ngModel)]="searchCity" 
          name="searchCity"
        >
        <input 
          class="form-control me-2" 
          type="text" 
          placeholder="Search by District" 
          aria-label="Search District" 
          [(ngModel)]="searchdistrict" 
          name="searchdistrict"
        >
        <input 
          class="form-control me-2" 
          type="text" 
          placeholder="Search by State" 
          aria-label="Search State" 
          [(ngModel)]="searchstate" 
          name="searchstate"
        >
        <button class="btn btn-outline-light" type="submit">Search</button>
      </form>
    </div>
  </nav>


  <section class="white">
    <div class="container-fluid py-4">
      <article class="postcard dark blue" *ngFor="let police of total_policestation">
        <a class="postcard__img_link">
          <img class="postcard__img" [src]="environment.apiUrl + police.photo_upload" alt="Image Title" />
          
        </a>
        
        <div class="postcard__text ">
          
          <h1 class="postcard__title blue">{{police.name}}</h1>
          <div class="postcard__bar"></div>
          <h1 style="font-weight: bold; font-size: 20px;">Phone : {{police.telephone_no}}</h1>
          <h1 style="font-weight: bold; font-size: 20px;">Address : {{police.address}}</h1>
          <h1 style="font-weight: bold; font-size: 20px;">City : {{police.city}}</h1>
          
          <ul class="postcard__tagbox">
            <li class="tag__item " (click)="seeMores(police)"><i class="fas fa-info mr-2" ></i> More Info</li>
            <li class="tag__item " (click)="seeChowikis(police)">
                          <i class="fa fa-cog fa-spin fa-2x fa-fw"></i>
                          <span class="sr-only">Loading...</span> See chowikis under</li>
          </ul>
        </div>
      </article>
      <div *ngIf="total_policestation?.length > 0">
        <app-pagination
          [currentPage]="pagination.current_page"
          [totalPages]="pagination.total_pages"
          [hasPrevious]="pagination.has_previous"
          [hasNext]="pagination.has_next"
          (pageChange)="onPageChange($event)">
        </app-pagination>
        </div>
        <div *ngIf="total_policestation?.length === 0">
        <p>No Police Station Available.</p>
        </div>
    </div>
  </section>
</div>