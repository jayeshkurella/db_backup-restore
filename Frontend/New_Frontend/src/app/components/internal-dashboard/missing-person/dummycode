<div class="container-fluid mt-2">
      <div class="tab-content mt-1">
        <!-- Pending Tab -->
      <div class="tab-pane fade show active" id="pending">
        <div class="table-responsive">
          <!-- Show message when no persons are found -->
          <div *ngIf="pendingPersons && pendingPersons.length === 0" class="text-center text-danger mt-3">
            No pending persons found with the applied filters.
          </div>

          <!-- Show table only if persons exist -->
          <table *ngIf="pendingPersons && pendingPersons.length > 0" class="table table-bordered mt-1">
            <thead class="table-dark">
              <tr>
                <th>SR NO.</th>
                <th>PHOTO</th>
                <th>FULL NAME</th>
                <th>AGE</th>
                <th>GENDER</th>
                <th>MISSING DATE</th>
                <th>ACTION</th>
              </tr>
            </thead>
            <tbody>
              <tr *ngFor="let person of pendingPersons; let i = index">
                <td>{{ i + 1 }}</td>
                <td>
                  <img [src]="person.photo_photo ? (environment.apiUrl + person.photo_photo) : 'assets/images/pngwing.com.png'" 
                      alt="person Photo" class="img-fluid rounded" 
                      style="width: 60px; height: 60px; object-fit: cover;" />
                </td>
                <td>{{ person.full_name }}</td>
                <td>{{ person.age }}</td>
                <td>{{ person.gender }}</td>
                <td>{{ person.date_reported }}</td>
                <td><button class="btn btn-outline-dark orange" (click)="viewDetails(person)">View</button></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
      <!-- Resolved Tab -->
      <div class="tab-pane fade" id="resolved">
        <div class="table-responsive">
          <!-- Show message when no resolved persons are found -->
          <div *ngIf="resolvedPersons && resolvedPersons.length === 0" class="text-center text-muted mt-3">
            No resolved cases found.
          </div>

          <!-- Show table only if resolved persons exist -->
          <table *ngIf="resolvedPersons && resolvedPersons.length > 0" class="table table-bordered mt-1">
            <thead class="table-dark">
              <tr>
                <th>SR NO.</th>
                <th>PHOTO</th>
                <th>FULL NAME</th>
                <th>AGE</th>
                <th>GENDER</th>
                <th>MISSING DATE</th>
                <th>ACTION</th>
              </tr>
            </thead>
            <tbody>
              <tr *ngFor="let person of resolvedPersons; let i = index">
                <td>{{ i + 1 }}</td>
                <td>
                  <img [src]="person.photo_photo ? (environment.apiUrl + person.photo_photo) : 'assets/images/pngwing.com.png'" 
                      alt="person Photo" class="img-fluid rounded" 
                      style="width: 60px; height: 60px; object-fit: cover;" />
                </td>
                <td>{{ person.full_name }}</td>
                <td>{{ person.age }}</td>
                <td>{{ person.gender }}</td>
                <td>{{ person.date_reported }}</td>
                <td><button class="btn btn-outline-dark orange" (click)="viewDetails(person)">View</button></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>


      </div>
    </div>